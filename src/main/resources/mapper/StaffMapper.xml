<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="initech.mvc.mapper.StaffMapper">
    <!-- register 등록 -->
    <insert id="insertStaff" parameterType="initech.mvc.vo.StaffVO">
        INSERT INTO member
        (memberid, member_password, confirmPassword, memberName, memberDepartment, memberPosition, member_EmploymentDate, memberBirth, memberEmail, email_verifycode, TermsAgreed, applicationDate, updatedAt)
        VALUES
            (#{memberid}, #{member_password}, #{confirmPassword}, #{memberName}, #{memberDepartment}, #{memberPosition}, #{member_EmploymentDate}, #{memberBirth}, #{memberEmail}, #{email_verifycode}, #{TermsAgreed}, NOW(), NOW())
    </insert>

    <!-- list 조회 -->
    <resultMap id="staffResultMap" type="initech.mvc.vo.StaffVO">
        <result column="bt_idm" property="bt_idm"/>
        <result column="memberName" property="memberName"/>
        <result column="member_department" property="member_department"/>
        <result column="member_position" property="member_position"/>
        <result column="member_EmploymentDate" property="member_EmploymentDate"/>
        <result column="applicationDate" property="applicationDate"/>
        <!-- 기타 필드 매핑 -->
    </resultMap>

    <!--list 페이징 조회 -->
    <select id="selectAllUsers" resultMap="staffResultMap" parameterType="map">
        SELECT * FROM member ORDER BY bt_idm LIMIT #{size} OFFSET #{offset}
    </select>

    <!-- list 전체 데이터의 수를 조회하는 쿼리 -->
    <select id="countAllUsers" resultType="int">
        SELECT COUNT(*) FROM member
    </select>

    <!-- 상세 조회 쿼리 -->
    <select id="UsersDetail" parameterType="long" resultType="initech.mvc.vo.StaffVO">
        SELECT memberName, memberBirth, memberid, memberEmail, memberDepartment, memberPosition
        FROM member
        WHERE bt_idm = #{bt_idm}
    </select>








</mapper>
